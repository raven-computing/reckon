# Configuration options for the Tree-Sitter-Java dependency

# Define the program variable to avoid errors when custom command
# is executed since the CLI might not actually be available.
set(TREE_SITTER_CLI "")
set(BUILD_TESTING OFF)
